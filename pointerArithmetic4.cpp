// to cmpare two pointers of same data type.
//not just one but all sore of comparisions can be performed.
#include <iostream>
using namespace std;

int main () {
    int* ptr1;
    int* ptr2; 
     cout << ptr1 << endl;
     cout << ptr2 << endl;

     cout << (ptr1 > ptr2); // checking this condition.We get 0 as false mesans this condition is false.
     return 0;
}